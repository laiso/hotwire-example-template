<h1>Messages</h1>

<% @messages.each do |message| %>
  <article>
    <%= message.content %>
  </article>
<% end %>

<% if @page.next %>
  <%= link_to pagy_url_for(@page, @page.next), rel: "next" do %>
    Next page
  <% end %>
<% end %>
